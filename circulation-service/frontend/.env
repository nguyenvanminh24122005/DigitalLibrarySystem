# API Gateway URL - chỉ dùng khi chạy trên host/ngrok
# Khi chạy localhost: frontend tự động dùng http://localhost:5002 (không cần sửa)
# Khi kết nối host: cập nhật URL này sang host gateway URL
VITE_API_GATEWAY_URL=https://starry-glutinous-ultimatum.ngrok-free.dev
